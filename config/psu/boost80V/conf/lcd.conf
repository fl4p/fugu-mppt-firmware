# LcdService: I2C status display (requires i2c_sda/i2c_scl in board.conf)
enabled=1
log_level=info
# addr: I2C address of the PCF8574 backpack (hex ok). If unset or not responding,
# probes the standard {0x27, 0x3F}.
#addr=0x27
