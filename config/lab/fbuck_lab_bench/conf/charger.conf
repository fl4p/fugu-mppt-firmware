# Battery/emulator bench profile. This remains the 29 V charge/OV reference; do not raise it to
# accommodate an open-output scope sweep because doing so also raises the protection threshold
# when a battery is attached. Use fbuck_lab_bench_open_output for that explicitly different rig.
vout_max=29
